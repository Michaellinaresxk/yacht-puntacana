/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
};

module.exports = nextConfig;

module.export = {
	i18n: {
		locales: ["es", "en"],
		defaultLocale: "en",
	},
};
